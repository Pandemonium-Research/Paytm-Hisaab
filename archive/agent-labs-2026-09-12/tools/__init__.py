"""Phinite custom tool handlers.

Each module is self-contained on purpose: Dev Studio takes one file per tool, so the small
HTTP helper is repeated rather than shared. Test them locally with `python -m tools.run_local`.
"""
